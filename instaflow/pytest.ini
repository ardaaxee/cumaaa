[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = -q --strict-markers
markers =
    unit: bağımsız birim testleri
    integration: birden fazla katmanı birlikte çalıştıran testler
filterwarnings =
    ignore::DeprecationWarning
